# Unified Feedback Model (Internal)

This skill normalizes feedback from different reviewers into a simple common shape internally:

- file + line/range
- severity / confidence (when available)
- summary of the concern
- suggested action (when provided)
- thread / comment ID for resolution

The exact source format is irrelevant. We care about the intent of the feedback.
